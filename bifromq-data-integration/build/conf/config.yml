integratorConfig:
  clientNum: ${CLIENT_NUM}
  topicFilter: ${TOPIC_FILTER}
  host: ${HOST}
  port: ${PORT}
producerConfig:
  type: Kafka # Kafka or Dummy
  kafkaConfig:
    bootstrapServers: ${SERVERS}